import React, { useEffect, useRef, useState } from 'react';
import {
  MediaStream,
  RTCPeerConnection,
  mediaDevices,
} from 'react-native-webrtc';

const useWebRTC = () => {
  useEffect(() => {}, []);

  return {};
};

export default useWebRTC;
